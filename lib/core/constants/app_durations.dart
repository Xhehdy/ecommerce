/// Animation and transition duration tokens.
class AppDurations {
  static const Duration instant = Duration(milliseconds: 100);
  static const Duration fast = Duration(milliseconds: 180);
  static const Duration normal = Duration(milliseconds: 300);
  static const Duration slow = Duration(milliseconds: 500);
  static const Duration splash = Duration(milliseconds: 1800);
  static const Duration splashHold = Duration(milliseconds: 800);
  static const Duration shimmerCycle = Duration(milliseconds: 1500);
}
